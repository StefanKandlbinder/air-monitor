// Empty module — replaces Next.js's pre-compiled polyfill-module.js.
// Array.at, Object.hasOwn, flatMap, Object.fromEntries, etc. are Baseline
// features natively supported in all browsers since 2022.
